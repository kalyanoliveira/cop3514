#include <iostream>

using namespace std;

int main(void)
{
    // This is actually so clutch. You don't have to declare any size.
    string msg;

    cout << "Enter a message: ";
    cin >> msg;
    cout << msg << endl;
    return 0;
}
